[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Pour React Router (si jamais tu en utilises un)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

